import { Box, Typography } from "@mui/material";

const Character = function() {
    return (
        <Box component="section">
            <Typography component="h2">Character</Typography>
        </Box>
    );
};

export default Character;